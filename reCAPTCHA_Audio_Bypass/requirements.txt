# ============================================================
# requirements.txt — reCAPTCHA v2 Audio Bypass System
# ============================================================
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# ⚠️  IMPORTANT — FFmpeg is required for pydub MP3→WAV conversion.
#     See FFmpeg installation instructions in the README or final notes.
# ============================================================

# Browser automation
playwright>=1.44.0

# Audio processing: MP3 → WAV conversion
# Requires FFmpeg to be installed on the system (not a Python package)
pydub>=0.25.1

# Speech recognition (uses Google Speech API — FREE, no key needed)
SpeechRecognition>=3.10.0

# HTTP client to download audio files
requests>=2.31.0

# ============================================================
# AFTER INSTALLING, also run:
#   playwright install chromium
# ============================================================
